class RenderQueue 
{
    void addGeometryBuffer(const CEGUI::GeometryBuffer& buffer);
    void removeGeometryBuffer(const CEGUI::GeometryBuffer& buffer);

    void draw() const;
    void reset();
};

